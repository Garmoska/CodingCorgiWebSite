---
interface Props {
  title: string;
  description: string;
  publishedDate: Date;
  slug: string;
  linkedinUrl?: string | undefined;
  tags?: string[] | undefined;
}

const { title, description, publishedDate, slug, linkedinUrl, tags } = Astro.props;

const formattedDate = new Intl.DateTimeFormat('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric'
}).format(publishedDate);
---

<article class="bg-white border border-gray-200 rounded-lg shadow-md hover:shadow-lg transition-shadow p-6">
  <a href={`/posts/${slug}`} class="block group">
    <h2 class="text-2xl font-bold text-gray-900 mb-2 group-hover:text-corgi-orange transition-colors">
      {title}
    </h2>
    <p class="text-gray-600 mb-4">{description}</p>
    <div class="flex items-center justify-between text-sm text-gray-500">
      <time datetime={publishedDate.toISOString()}>
        {formattedDate}
      </time>
      {linkedinUrl && (
        <a
          href={linkedinUrl}
          target="_blank"
          rel="noopener noreferrer"
          class="text-blue-600 hover:text-blue-800 flex items-center gap-1"
        >
          View on LinkedIn →
        </a>
      )}
    </div>
    {tags && tags.length > 0 && (
      <div class="flex flex-wrap gap-2 mt-4">
        {tags.map(tag => (
          <span class="bg-corgi-cream text-corgi-brown text-xs font-semibold px-3 py-1 rounded-full">
            {tag}
          </span>
        ))}
      </div>
    )}
  </a>
</article>
